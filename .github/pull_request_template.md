## Summary

<!-- What does this PR change and why? -->

## Test plan

- [ ] `./mvnw -B test` (or `make test`)
- [ ] `./mvnw -B checkstyle:check` (or `make lint`) if Java sources changed
- [ ] `./mvnw -B verify` for coverage-sensitive changes

## Checklist

- [ ] No secrets or `.env` files committed
- [ ] CHANGELOG.md updated if user-facing behavior changed
- [ ] Docs / README updated if needed
